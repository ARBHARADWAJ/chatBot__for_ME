{
  "name": "cogniflow-master",
  "version": "1.0.0",
  "description": "Master script to run the full stack AI application",
   "scripts": {
    "install:all": "npm install && cd Backend && npm install && cd ../Frontend && npm install",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:clean": "docker-compose down --rmi all -v",
    "backend": "cd Backend && npm run dev",
    "frontend": "cd Frontend && npm run dev",
    "dev": "npm run docker:up && concurrently --kill-others-on-fail \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}